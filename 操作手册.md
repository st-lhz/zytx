## 卓影童行 - 操作手册

### 更新网站（修改完 src/data.json 或页面样式后）

在项目根目录执行：

```powershell
npm run build
git add docs
git commit -m "update: 更新站点内容"
git push
```

### GitHub Pages 设置（只需设置一次）

- Settings → Pages
- Build and deployment → Source：Deploy from a branch
- Branch：main
- Folder：/docs
